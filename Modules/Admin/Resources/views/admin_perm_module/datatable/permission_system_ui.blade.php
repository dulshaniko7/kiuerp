var permission_system = full["permission_system"];

var uiText = '<div class="clearfix">';

    if(permission_system !== null)
    {
        uiText += permission_system.system_name;
    }
    uiText += '</div>';

return uiText;
